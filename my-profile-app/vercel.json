{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "backend/package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/src/main.ts" },
    { "src": "/(.*)\\.(?:js|css|png|jpg|jpeg|svg|gif|webp|ico|woff|woff2|ttf|eot)$", "dest": "/frontend/dist/$1" },
    { "src": "/favicon.ico", "dest": "/frontend/dist/favicon.ico" },
    { "src": "/(.*)", "dest": "/frontend/dist/index.html", "status": 200 }
  ]
}
